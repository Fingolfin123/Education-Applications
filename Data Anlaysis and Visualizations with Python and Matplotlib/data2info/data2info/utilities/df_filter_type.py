import pandas
#Set filter type based on input

def set_filter(type, a, b)
	#">" | "<" | "==" | ">=" | "<=" | "!=" | "is" ["not"] | ["not"] "in"
    match type:
        case 400:
            df = 
        case 404:
            return "Not found"
        case 418:
            return "I'm a teapot"
        case _:
            return "Something's wrong with the internet"